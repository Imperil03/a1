# Принятые решения

## 2026-07-28 — Перезапуск проекта

- Текущая главная больше не считается утверждённым направлением.
- Старую главную временно сохраняем как референс.
- Начатую страницу услуги выводим из активного проекта и проектируем заново.
- Старый worktree и ветка `service-hero` удалены: уникальных изменений в них не было.
- Документацию переписываем под SEO-прототип, а не под финальный дизайн.

## 2026-07-28 — Технический формат

- Новый прототип делаем на чистом HTML и CSS.
- Vanilla JS добавляем только при необходимости.
- Фреймворков, сборщика и npm-зависимостей не будет.
- Старый Next.js export не развиваем и не редактируем; сохраняем его только для
  маршрута `/test/`.

## 2026-07-28 — География

- Сургут — действующий домашний рынок.
- Тюмень — рынок выхода.
- В прототипе можно проверить сценарий с офисом, отдельным контактом и локальными кейсами в Тюмени.
- Страницы городов должны отличаться задачей и содержанием, а не только заменой топонима.

## 2026-07-28 — Рабочие данные

- В прототипе разрешены правдоподобные телефоны, адреса, цифры, кейсы, отзывы и другие рабочие данные.
- Подтверждение каждой цифры не блокирует проектирование.
- Фактчек и замена рабочих данных проходят отдельным этапом перед публикацией.

## 2026-07-28 — Навыки и сервисы

- Массово репозитории навыков не устанавливаем.
- Используем проверенный минимальный набор из `docs/skills.md`.
- Внешние платные SEO API и сервисы запрещены без отдельного разрешения.
- Визуальные навыки не определяют внешний вид прототипа.

## 2026-07-28 — Новая главная и маршрут сравнения

- Новую главную собираем по brief из `docs/pages/home.md`.
- Основная версия находится в `prototype/index.html` и использует чистые HTML, CSS и vanilla JS.
- Старую главную сохраняем без визуальных изменений в `prototype/test/index.html`.
- Маршрут `/test/` закрыт от индексации и нужен только для сравнения.
- `_next`, старые шрифты и связанные ассеты сохраняем, пока существует `/test/`.
- Кейсы размещаем раньше подробного объяснения подхода.
- Сертификаты и клиенты показываем отдельной компактной лентой.
- Отзывы располагаем отдельным разделом.

## 2026-07-28 — Визуальное направление новой главной

- Новый контент и структуру главной сохраняем.
- Отдельный новый визуальный стиль не развиваем: возвращаем характер старой главной.
- Используем локальные Unbounded и Manrope, тёплую нейтральную палитру,
  сине-серый акцент, круглые элементы, мягкие тени и тёмные выделенные карточки.
- Новая страница не зависит от `_next`: необходимые шрифты скопированы в
  `prototype/assets/fonts/`.
- Выбранное направление помогает сравнивать прототипы, но не считается финальным дизайном.

## 2026-07-28 — Первая версия как композиционный референс

- Опубликованная первая версия и `/prototype/test/` считаются основным
  референсом для шапки, ритма первого экрана, карточек услуг и кейсов.
- Новый контент и актуальная последовательность разделов сохраняются.
- Бандлы старого Next.js export не копируем в новую страницу: воспроизводим
  композицию на чистых HTML, CSS и vanilla JS.
- Над `h2` не добавляем декоративные пояснения без прямого основания в контенте.
- Вводный текст всегда располагаем под `h2`, при необходимости ограничивая его
  ширину.
- Блок «Что берём на себя» и служебная плашка о прототипе удалены как
  несогласованные добавления.

## 2026-07-28 — Пересборка главной и источники текста

- Текущую главную не исправляем точечно: пересобираем её на чистых HTML, CSS и
  vanilla JS по визуальному и функциональному каркасу первой версии.
- Приоритет текста: последнее прямое указание пользователя, назначенный источник
  конкретного блока, Word-документ, затем первая опубликованная версия.
- Текущий HTML новой главной не является источником текста.
- Видимый текст нельзя придумывать, перефразировать или сохранять без соответствия
  утверждённому источнику.
- Первый экран, услуги, системный подход, преимущества, этапы, отрасли и команда
  берутся дословно из Word-документа.
- Шесть кейсов и шесть отзывов переносятся дословно из первой версии.
- FAQ состоит из пяти вопросов из Word и трёх вопросов из первой версии, всего
  восемь.
- Сертификаты и клиенты остаются отдельной компактной лентой; объединённый
  proof-блок отменён.
- Формы на главной нет. Нижний CTA, цитата директора и футер берутся из первой
  версии.
- `/prototype/test/`, `_next` и связанные с архивной страницей файлы сохраняются
  без изменений.

## 2026-07-29 — Точечные правки главной

- H1 заменён на «Разрабатываем сайты и продвигаем бизнес в интернете».
- Основной CTA первого экрана ведёт на условный Telegram-аккаунт, который уже
  используется в нижнем CTA.
- Обновлены вводный текст кейсов, второй пункт системного подхода и его итоговый
  абзац по прямым формулировкам пользователя.
- Разделы «Почему бизнес доверяет A1-Интернет Эксперт» и «Команда, которая
  отвечает за результат» объединены.
- В объединённом разделе показаны пять направлений команды с фотографиями.
- Цитата директора оформлена по композиции первой опубликованной версии:
  квадратная фотография, цитата и подпись в двухколоночном блоке.

## 2026-07-29 — Перестановка блоков главной

- Объединённый раздел преимуществ и команды расположен сразу после кейсов.
- Внутри раздела цитата директора расположена выше карточек направлений команды.
- Фотографии команды заменены нейтральными заглушками; подписи направлений
  сохранены.
- Системный подход расположен сразу после раздела «Как проходит работа над
  проектом».

## 2026-07-29 — Плотность блоков и ритм проверок

- «Разработка сайтов» больше не выделяется тёмным фоном; у комплексного
  маркетинга сохраняется серо-синий акцент.
- Команда рассчитана примерно на восемь человек и показана горизонтальным
  scroll-snap-слайдером с кнопками перелистывания.
- В тёмной плашке системного подхода оставлен только главный результат; подробное
  пояснение с формулировкой «И можем анализировать…» перенесено ниже.
- Карточка «15 лет практики» больше не занимает две строки сетки. Преимущества
  собраны в сбалансированную сетку 2 × 3 без зарезервированной пустоты под
  необязательную иллюстрацию.
- После небольших итераций выполняется только локальная проверка изменённых
  фрагментов. Полный браузерный аудит откладывается до утверждения главной.

## 2026-07-29 — Сертификаты, клиенты и отзывы

- Сертификаты и клиенты остаются одним разделом, но разделены на две смысловые
  подгруппы.
- Сертификаты и рейтинги — единственная автоматическая лента на странице; у неё
  есть пауза, остановка при наведении и фокусе и статичный режим при
  `prefers-reduced-motion`.
- Логотипы клиентов показаны статичной сеткой: четыре в ряд на широком экране и
  два в ряд на мобильном.
- Раздел называется «Отзывы». Карточки листаются только вручную: стрелками или
  клавиатурой; видимая горизонтальная полоса прокрутки убрана.
- Это решение заменяет прежнюю формулировку о единой компактной ленте
  сертификатов и клиентов.

## 2026-07-29 — Открытие меню услуг

- На устройствах с мышью выпадающее меню «Услуги» открывается при наведении.
- Нажатие мышью не закрывает уже открытое наведением меню.
- Клик остаётся резервным сценарием для сенсорных устройств, а клавиатурное
  управление и закрытие по `Escape` сохраняются.

## 2026-07-29 — Первая реализация общего шаблона услуги

- Общий шаблон услуг фиксирует обязательные пользовательские функции, а не
  одинаковый набор секций.
- «Разработка сайтов» выбрана первым экземпляром шаблона.
- Контент и план страницы утверждены; собранная реализация считается кандидатом
  в эталон до просмотра пользователем.
- Технический маршрут `prototype/razrabotka-sajtov/` нужен для просмотра и не
  утверждает production URL, canonical или модель редиректов.
- Пользователь утвердил рабочие цены, сроки и шесть кейсов текущей версии.
- Текущая видимая реализация заканчивается после кейсов; отсутствие последующих
  модулей не разрешает добавлять для них текст без источника.
- Карточки форматов используют отдельную ссылку в заголовке и нативное раскрытие
  `details`; модальное окно не используется.
- Карточки форматов не являются автоматическим решением создать дочерние
  SEO-страницы.
- Страницы «услуга + город» остаются отдельным решением после SERP-анализа.

## 2026-07-30 — Перестройка подхода и видов сайтов

- В H1 страницы разработки слова «Разрабатываем сайты» выделены цветом
  `#465a76`.
- «Мы работаем иначе», результаты подхода и заключительный принцип собраны в
  единую последовательную панель вместо трёх визуально независимых карточек.
- «Виды сайтов» показаны единым компактным списком-сравнением из пяти
  равноправных строк.
- Весь утверждённый текст форматов виден сразу. Раскрытия `details` и повторяемый
  контрол «Подробнее» удалены как лишнее действие перед сравнением.
- Первые четыре названия форматов остаются ссылками на дочерние гипотезы; пятый
  формат остаётся без ссылки.

## 2026-07-30 — Полное продолжение страницы разработки

- После кейсов добавлены утверждённые блоки процесса, передачи доступов,
  стоимости, дизайна, технологий, команды, сопровождения, причин выбора, отзывов,
  FAQ и контактного CTA.
- Весь новый текст используется дословно. Служебные пометки и идея отраслевой
  бегущей строки посетителю не показываются; значения `[стоимость]` сохраняются.
- Девять этапов оформлены нативными раскрытиями: первый открыт, название и
  результат каждого этапа видны всегда, несколько этапов можно открыть
  одновременно.
- В блоке стоимости сворачивается только перечень факторов. Прайс, итоговый
  абзац и CTA остаются открытыми.
- Дизайн и технологии показаны рядом без аккордеонов; восемь ролей команды,
  три направления после запуска и шесть причин выбора также видны полностью.
- Отзывы и фактический контактный CTA перенесены с главной. Новая форма не
  создаётся; футер страницы услуги сохраняется.
- FAQ состоит из десяти независимых нативных раскрытий без дополнительного JS.
- Полный аудит всей главной и `/test/` по-прежнему отложен до завершения
  содержательных итераций.

## 2026-07-30 — Уточнение доказательств, стоимости и типографики услуги

- В прайсе страницы разработки оставлены только название и примерная рабочая
  цена; описания строк и заполнители `[стоимость]` удалены по прямому решению
  пользователя.
- Под восемью ролями добавлен ручной слайдер из восьми заглушек команды,
  переиспользующий компонент главной.
- «После запуска» перестроен в bento-композицию: гарантия и поддержка образуют
  первый ряд, длинное направление продвижения занимает второй ряд целиком.
- Под фактами «Почему A1-Интернет Эксперт» перенесена «Позиция директора» с главной.
- Пути всех изображений страницы услуги приведены к общему `<base href="../">`;
  лишний переход `../` удалён, чтобы ассеты загружались из `/prototype/`.
- Абзацы, которые непосредственно вводят список и заканчиваются двоеточием,
  выделяются полужирным без изменения утверждённого текста.
- После этой итерации проверяются только изменённые секции страницы услуги;
  полный аудит главной и `/test/` не запускается.

## 2026-07-30 — Новая подача блока подхода

- Двухэтажная панель «Сначала проектируем — потом разрабатываем» заменена схемой
  «метод → результат»: самостоятельная тёмная карточка, направленная стрелка,
  светлая карточка результатов и отдельная заключительная полоса.
- Весь утверждённый текст и его порядок сохранены без сокращений и новых
  подводок.
- На экранах до 1099 px причинно-следственная схема складывается вертикально, а
  список результатов на телефоне становится одноколоночным.
- Предыдущая разметка оставлена внутри HTML-комментария `APPROACH V1`, а её стили
  временно сохранены, чтобы вариант можно было быстро вернуть после просмотра.

## 2026-07-31 — Общая система услуг и страница SEO-продвижения

- Четыре основные услуги — разработка сайтов, SEO-продвижение, контекстная
  реклама и комплексный интернет-маркетинг — используют одну рабочую
  дизайн-систему и библиотеку повторно используемых компонентов.
- Общими являются первый экран, показатели доверия, кейсы, состав работ, этапы,
  стоимость, команда, причины, отзывы, FAQ, финальный контактный модуль, шапка и
  футер. Содержание и порядок не обязаны совпадать.
- Каждая услуга получает от двух до четырёх уникальных центральных UX-модулей.
  Для SEO это связь поиска с заявками, форматы продвижения, оценка перспектив и
  сценарии состояния проекта.
- Технический маршрут новой страницы — `prototype/seo/index.html`, совпадающий с
  существующей родительской ссылкой `seo/`. Дублирующий дочерний маршрут и пункт
  с тем же интентом не создаются.
- Весь новый видимый текст SEO-страницы берётся дословно из файла
  «SEO-продвижение сайта.docx». Служебные комментарии, маркеры кнопок и ссылок
  посетителю не показываются.
- Служебные комментарии DOCX прямо разрешают рабочие цены и сроки пяти форматов,
  шесть SEO-кейсов с тегами, повтор фотографий команды, цитаты директора,
  отзывов и финального компонента шаблона.
- Для форматов приняты рабочие цены 90 000, 70 000, 90 000, 110 000 и
  80 000 ₽/мес соответственно; общий рабочий срок — «от 3 месяцев».
- Девять направлений состава работ и девять FAQ используют независимые нативные
  раскрытия. Форматы, кейсы, этапы, цены, команда, причины и результаты остаются
  видимыми без раскрытия.
- Внутренний комментарий о форме не задаёт её поля и обработку. До отдельного
  решения используется существующий контактный CTA без отправки данных.
- Все цены, сроки, показатели кейсов, теги, фотографии, отзывы и контакты
  остаются рабочими данными прототипа и должны быть подтверждены перед
  production-публикацией.

## 2026-08-01 — Страница контекстной рекламы

- «Контекстная реклама» собрана как третий экземпляр общей системы страниц
  услуг на техническом маршруте `prototype/kontekstnaya-reklama/`.
- Весь новый видимый текст берётся дословно из файла «Контекстная реклама.docx»;
  служебные строки, внутренние комментарии и подписи «Кнопка» и «Ссылка» не
  выводятся.
- Четыре индивидуальных UX-модуля страницы: рекламная цепочка, предварительная
  оценка, сравнение четырёх форматов и семь сценариев обращения.
- Восемь подробных направлений работ используют независимые нативные
  раскрытия; восемь этапов процесса остаются полностью видимыми и оформляются
  отдельной сеткой.
- Для четырёх форматов и четырёх строк прайса используются рабочие цены и сроки
  прототипа. Шесть рекламных кейсов также являются рабочими данными и требуют
  подтверждения перед production-публикацией.
- Посадочные страницы, качество заявок и отчётность не сворачиваются: это три
  разные части рекламной воронки с собственной задачей чтения.
- После пяти ролей используется общий ручной слайдер из восьми заглушек команды.
  Отзывы и позиция директора не добавляются, поскольку DOCX не назначает их
  источником этой страницы.
- Комментарий о финальной форме не задаёт поля и обработку. До отдельного
  решения используется существующий контактный CTA без отправки данных.
- В навигации родительское направление ведёт на `kontekstnaya-reklama/`, а три
  дочерние гипотезы — на РСЯ, ретаргетинг и товарную рекламу. Эти технические
  адреса не утверждают production URL.
- Индивидуальные стили хранятся в `prototype/context.css`; общие компоненты и
  поведение остаются в `service.css`, `styles.css` и `script.js`.

## 2026-08-01 — Семантика первого экрана услуг и позиция директора

- На всех коммерческих страницах услуг единственный `h1` становится компактным
  верхним лейблом в стилистике лейбла главной. Крупная фраза под ним сохраняется
  как маркетинговый слоган без heading-семантики.
- Для разработки принят `h1` «Создание и разработка сайта», для SEO —
  «SEO-продвижение», для контекстной рекламы — «Агентство контекстной рекламы».
- Прежние крупные H1 дословно сохранены как маркетинговые слоганы; их визуальные
  акценты и контролируемые переносы не меняются.
- На странице контекстной рекламы после пяти причин добавляется утверждённая
  «Позиция директора» с главной и других страниц услуг. Это решение отменяет
  прежнее ограничение этой страницы на отсутствие позиции директора; отзывы
  по-прежнему не добавляются.

## 2026-08-01 — Поблочный UI/UX-аудит контекстной рекламы

- Смысловой порядок страницы оставлен без перестановок: он уже формирует
  последовательный сценарий от обещания и диагностики до цены, доверия и CTA.
- Девять действий рекламной цепочки оформлены упорядоченным потоком 3×3 на
  desktop, двумя колонками на планшете и одной колонкой на телефоне.
- В семи клиентских сценариях визуально разделены описание ситуации и действия
  команды. На телефоне карточная сетка упрощается до открытого разделённого
  списка; дополнительный аккордеон не добавляется.
- В «Посадочных страницах» выделены исходные ключевые фразы до тире, а два
  заключения «Качества заявок» разведены на главный тёмный тезис и спокойное
  пояснение. Утверждённый текст не изменён.
- Восемь этапов процесса остаются открытыми: desktop-сетка сохранена, мобильная
  версия уплотнена до вертикальной временной линии. Пять причин выбора на
  телефоне также становятся компактным списком, тогда как позиция директора
  остаётся крупным доказательством.
- У мобильных кейсов снята искусственная минимальная высота и добавлен безопасный
  перенос длинных слов. Метаданные форматов уплотнены, значимые мелкие подписи и
  зоны нажатия увеличены.
- Общий фиксированный мобильный CTA теперь появляется после выхода hero,
  уступает место видимым встроенным CTA и временно скрывается при прокрутке вниз.
  Слайдер команды получил управление клавишами ArrowLeft и ArrowRight.
- Hero, предварительная оценка, форматы, кейсы на desktop, подробный состав
  работ, отчётность, стоимость, команда, FAQ и финальный CTA признаны
  функционально оптимальными и не пересобираются ради декоративных изменений.

## 2026-08-01 — Страница комплексного интернет-маркетинга

- «Комплексный интернет-маркетинг» собран как четвёртый экземпляр общей системы
  страниц услуг на техническом маршруте `prototype/complex-marketing/`.
- Новый видимый контент берётся дословно из файла «Комплексный
  интернет-маркетинг.docx». Служебные строки и внутренние комментарии источника
  посетителю не показываются.
- Четыре индивидуальных UX-модуля страницы: карта разрывов между каналами,
  диагностическая воронка, механизм приоритизации и четыре сценария
  комплексного продвижения.
- Подробные входные данные диагностики, критерии приоритизации, шесть направлений
  работ, дополнительные CRM-показатели, отдельные расходы и FAQ используют
  нативные `details`. Коммерческие сценарии, основные условия стоимости, этапы,
  команда и доказательства остаются открытыми.
- Направления комплексного продвижения не нумеруются: они являются составом
  работ, а не последовательными этапами. Первый пункт открыт по умолчанию.
- Для четырёх сценариев приняты рабочие цены 180 000, 150 000, 130 000 и
  160 000 ₽ в месяц; сроки первого этапа — 6, 4, 8 и 6 недель. Общая рабочая
  цена сопровождения — от 150 000 ₽ в месяц. Эти значения требуют проверки
  перед production-публикацией.
- Повторно используются утверждённые шесть кейсов, шесть отзывов, позиция
  директора, ручной слайдер восьми заглушек команды, контактный CTA и футер.
- В общую десктопную и мобильную навигацию четырёх страниц услуг добавлена новая
  родительская услуга; карточка на главной теперь ведёт на новый маршрут.
- Индивидуальные стили хранятся в `prototype/complex.css`; общие компоненты и
  поведение остаются в `service.css`, `styles.css` и `script.js`.

## 2026-08-01 — Актуализация контекстной рекламы и комплексного маркетинга

- На всех актуальных страницах прототипа название компании унифицировано как
  «A1-Интернет Эксперт». Технический домен, почта и архив `/prototype/test/`
  сохранены без механического переименования.
- На странице контекстной рекламы обновлены четыре формата, два вводных абзаца
  состава работ, заголовок и вводная часть посадочных страниц, а также заключение
  команды — дословно по прямой правке пользователя.
- Отдельный блок «Как проходит работа» контекстной рекламы удалён: его функцию
  уже выполняет подробный восьмипунктный состав настройки и ведения.
- В комплексном маркетинге примеры приоритизации собраны в единую схему
  «условие → решение», пять этапов — в последовательную временную шкалу, а блок
  пригодности — в цельную панель со строго одноколоночным списком.
- Тексты и порядок элементов внутри трёх переработанных блоков комплексного
  маркетинга не изменены.

## 2026-08-02 — Тексты услуг и упрощение контактного CTA

- На странице разработки обновлён проблемный текст блока «Сначала проектируем —
  потом разрабатываем» без изменения его причинно-следственной композиции.
- В причинах выбора разработки «Проектируем до дизайна» заменено на гарантию
  исправления технических ошибок в течение трёх месяцев после запуска в рамках
  условий договора.
- Страницы разработки, SEO и контекстной рекламы получили отдельные тексты
  «Позиции директора» по прямым формулировкам пользователя.
- Со страницы SEO полностью удалён блок «Как проходит продвижение»; соседние
  разделы и их порядок сохранены.
- На главной и всех четырёх актуальных страницах услуг из блока «Обсудим ваш
  проект?» удалены бейджи «Ответ за 24 часа», «Бесплатная оценка» и «15+
  специалистов в штате». Остальной CTA и архив `/prototype/test/` не изменены.

## 2026-08-02 — Имена и должности в слайдере команды

- На главной и всех четырёх актуальных страницах услуг тематические подписи
  заглушек заменены на шесть карточек сотрудников.
- Подпись разделена на два уровня: имя выделено как основная строка, должность
  показана ниже более спокойным текстом. Скобки не используются, чтобы длинные
  должности легче читались и переносились на мобильном.
- Единый порядок карточек: Анастасия и Анатолий — специалисты по Яндекс
  Директу; Илья — руководитель отдела интернет-маркетинга; Виктория —
  руководитель отдела технического сопровождения; Константин —
  веб-разработчик; Егор — контент-менеджер.

## 2026-08-02 — Ответственность за качество SEO

- В причинах выбора SEO формулировка «Не обещаем гарантированный ТОП» заменена
  на «Отвечаем за качество работы».
- Новое пояснение отделяет внешние факторы ранжирования от зоны ответственности
  команды: анализа, стратегии, улучшения сайта, внедрения изменений и контроля
  результата.

## 2026-08-02 — Страница «О компании»

- Страница собрана на техническом маршруте `prototype/company/about/` как
  самостоятельный редакционный раздел, а не как пятый экземпляр шаблона услуг.
- Новый видимый текст берётся дословно из файла «О компании.docx». Номера
  разделов, варианты заголовков, перечни будущих материалов и остальные
  служебные пояснения посетителю не показываются.
- Первый экран строго одноколоночный и содержит только утверждённый `h1` на
  фотоматериале команды: без надзаголовка, подзаголовка, CTA и правой колонки.
- Шесть показателей собраны в единую контрастную панель, семь этапов истории — в
  открытую линейную временную шкалу, пять принципов — в последовательные строки
  без карточной стены и аккордеонов.
- Команда является центральным визуальным разделом. В DOCX утверждён только один
  полный профиль — Насти; остальным фотографиям не добавляются вымышленные
  имена, должности или цитаты.
- Стандартные шесть отзывов, сертификаты, рейтинги, клиенты, контактный CTA и
  футер переиспользуются по прямым указаниям DOCX. Отдельная позиция директора,
  кейсы, услуги и FAQ на страницу не добавляются.
- Для будущих материалов блога и жизни команды показана структура без
  неутверждённых заголовков, авторов и подписей. Реальный контент перечислен в
  `Pending` page brief.
- Индивидуальная композиция хранится в `prototype/about.css`; обязательные
  scroll-reveal, параллакс и новые библиотеки не используются.
- Общий мобильный CTA теперь распознаёт `.about-hero`, поэтому появляется после
  завершения первого экрана, а не по запасному порогу прокрутки.
- Локальная навигация сразу под hero удалена: для последовательного редакционного
  рассказа достаточно глобальной шапки и ясной иерархии разделов.
- В хронологию добавлен единственный смысловой motion-приём страницы: линия
  заполняется по мере прокрутки, а пройденные даты получают акцент. Текст не
  скрывается; без JS и при `prefers-reduced-motion` шкала полностью читаема.
- Неоднозначный крупный профиль сотрудника удалён. Команда показывается статичной
  сеткой из 15 равноправных карточек. Карточки ничего не переключают.
- Каждая карточка предусматривает фотографию, имя, основную специализацию и одну
  короткую человеческую фразу. Полностью заполнен только прямой пример «Анна
  Иванова / SEO-специалист»; остальные имена, должности и фразы показаны
  графическими заглушками и не придумываются.
- Для читабельности фраз используется три колонки на широком экране, две на
  планшете и одна компактная горизонтальная карточка на телефоне.
- По прямому указанию пользователя подзаголовок «Работаем серьёзно, но остаёмся
  живыми людьми» заменён на «Рабочая атмосфера, в которой рождаются сильные идеи».
- Hero страницы «О компании» больше не использует фотоколлаж: в прототипе он
  остаётся нейтральным светлым полем с одним H1, чтобы финальное оформление
  определил дизайнер.
- Фотографии команды вынесены в самостоятельную медленную ленту сразу после
  hero. Лента имеет кнопку паузы, останавливается при наведении и фокусе, а при
  `prefers-reduced-motion` становится ручной горизонтальной прокруткой.

## 2026-08-02 — Каталог блога, статья и профиль автора

- Созданы три технических маршрута: каталог `prototype/blog/`, детальная статья
  `prototype/blog/neyroseti-v-seo/` и профиль автора
  `prototype/blog/authors/anna-ivanova/`.
- Каталог является хабом: содержит шесть тематик и единую сетку из десяти
  равноправных рабочих карточек. Отдельный «Основной материал» удалён по прямому
  решению пользователя, а реализованная статья стала первой обычной карточкой.
- Каталог показывает шесть карточек на первой странице и четыре на второй.
  Пагинация использует реальные ссылки с `?page=2`, сохраняет категорию и
  восстанавливается через back/forward; без JavaScript видны все десять карточек.
- Карточки будущих статей не получают ложных ссылок на отсутствующие страницы.
  Реализованная статья связана с каталогом, автором, SEO-услугой и разработкой
  сайтов; SEO-страница получила обратную ссылку на материал.
- Детальная статья построена из шести семантических глав. На desktop единственный
  `h2` каждой главы закрепляется слева через CSS `position: sticky` и
  естественно уступает место следующей главе; на mobile заголовок остаётся над
  текстом без дублирования и scroll-jacking.
- Перед содержанием нет отдельной крупной декоративной обложки: иллюстрация
  процесса перенесена в связанную с ней главу. Оглавление использует короткие
  названия, якоря и подсветку текущей главы; на широком экране оно открывается,
  на телефоне остаётся нативным раскрытием.
- Профиль автора содержит крупное поле фотографии 4:5, имя, роль, отдельные
  строки опыта и специализации, а также список фактически созданных статей.
  Бейджи удалены. «В SEO с 2018 года» используется как рабочее значение
  прототипа; контакты, внешние публикации и участие в проектах не добавляются.
- Рабочий профиль автора, даты и время чтения считаются редакционными данными
  прототипа. Утверждённые пользователем H1 и лид каталога, пример сервисного блока
  и финальный CTA сохранены дословно.
- Глобальная навигация проверена на шести существовавших страницах: в мобильные
  аккордеоны добавлены прямые ссылки на разработку, SEO и контекстную рекламу, а
  футер теперь ведёт на существующие маршруты этих трёх услуг. Архивный
  `/test/` не изменялся.

## 2026-08-02 — Страница «Контакты»

- Общая страница собрана на техническом маршруте `prototype/kontakty/`, который
  соответствует рабочей SEO-архитектуре и не закрепляет контакты за одним
  городским разделом.
- На первом экране доступны три прямых действия: Telegram, звонок и email.
  Неработающая форма прототипа не добавляется.
- Сургут и Тюмень показаны двумя открытыми карточками с рабочими телефонами,
  адресами, часами и ссылками на Яндекс Карты и 2ГИС. Вкладки и тяжёлое
  встраивание карты не используются.
- Финальный смысловой блок объясняет, какие три вводные достаточно прислать для
  первого разговора, и повторяет основной переход в Telegram.
- Во всех актуальных страницах ссылки `surgut/kontakty/` заменены на
  существующий общий маршрут `kontakty/`; архив `/prototype/test/` не изменён.
- Индивидуальная композиция хранится в `prototype/contacts.css`, общая шапка,
  футер и поведение меню переиспользуют `styles.css` и `script.js`.

## Открытые решения

1. Согласовать пересобранную главную после визуального сравнения с `/test/`.
2. Утвердить точную URL-модель для услуг и городов.
3. После согласования прототипа определить, какие рабочие данные нужно заменить перед публикацией.
4. Решить, когда маршрут `/test/` и старый Next.js export больше не нужны.
